#!/bin/bash
cdls()
{
    \cd "$@" && ls
}
alias cd="cdls"

alias doc="cd ~/chakio_dockerfiles"
source $HOME/dotfiles/.docker_aliases
source $HOME/dotfiles/.of_aliases
source $HOME/dotfiles/.ros_aliases
source $HOME/dotfiles/.ssh_aliases
source $HOME/dotfiles/.git_aliases
